js { const constants = require("includes/constants"); }

config {
  type: "view",
  schema: constants.analyticsDataset,   -- ou analytics_dev si tu l’as hardcodé
  name: "src_raw_orders"
}

select *
from ${ref("orders")}