config {
  type: "view",
  tags: ["staging"],
  schema: constants.ANALYTICS_DATASET,
  name: "src_curated_customers_iceberg"
}

/**
 * SOURCE CURATED (Iceberg) :
 * - Les tables Iceberg sont stockées sur GCS
 * - Exposées à BigQuery via tables externes dans curated_ext_<env>
 */

select
  *
from
  `${dataform.projectConfig.defaultDatabase}.${constants.CURATED_EXT_DATASET}.customer`;