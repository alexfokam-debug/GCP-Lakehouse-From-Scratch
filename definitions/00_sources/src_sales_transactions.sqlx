const constants = require("includes/constants");
config {
  type: "declaration",
  database: dataform.projectConfig.defaultDatabase,
  schema: constants.rawExtDataset,
  name: "sales_transactions"
}

select
  *
from `lakehouse-486419.raw_ext_dev.sales_transactions`