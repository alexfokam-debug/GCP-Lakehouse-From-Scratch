config {
  type: "view",
  schema: "analytics",
  name: "stg_orders",
  description: "Staging view for raw orders"
}

SELECT
  order_id,
  customer_id,
  order_date,
  amount
FROM ${ref("src_raw_orders")}