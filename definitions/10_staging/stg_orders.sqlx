config {
  type: "view",
  schema: "analytics",
  name: "stg_orders",
  description: "Staging view for orders"
}

SELECT
  order_id,
  order_name,
  order_amount,
  order_ts,
  load_ts,
  record_source
FROM ${ref("src_raw_orders")}