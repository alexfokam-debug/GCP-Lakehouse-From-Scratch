config {
  type: "view",
  schema: "analytics_dev",
  name: "stg_customers",
  tags: ["staging","prod"]
}

-- temporaire si tu n'as pas encore de source customer :
select
  cast(null as string) as customer_id,
  cast(null as string) as customer_name,
  current_timestamp() as load_ts
where 1=0