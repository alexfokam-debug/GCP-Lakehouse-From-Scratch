name: Terraform - Reusable (enterprise)

# =============================================================================
# WORKFLOW REUTILISABLE (MOTEUR)
# -----------------------------------------------------------------------------
# Ce workflow ne se déclenche PAS tout seul.
# Il est appelé par dev/staging/prod via:
#   uses: ./.github/workflows/terraform-reusable.yml
#
# Objectifs (enterprise-grade) :
# - Mutualiser la logique Terraform pour dev/staging/prod
# - Auth GCP via Workload Identity Federation (OIDC GitHub -> WIF GCP)
# - Garde-fous :
#   - PR fork / Dependabot => secrets non injectés => on SKIP proprement
# - Robustesse :
#   - sanitize PROJECT_ID / TF_BUCKET avant tout appel gcloud
#   - terraform fmt sur *.tf uniquement (pas sur tfvars)
# =============================================================================

on:
  workflow_call:
    inputs:
      environment:
        description: "GitHub environment (dev|staging|prod)"
        required: true
        type: string
      action:
        description: "Terraform action (plan|apply)"
        required: true
        type: string

    # ENTERPRISE: on déclare explicitement les secrets attendus par le moteur
    # -> évite l'erreur "must specify exactly one of workload_identity_provider..."
    secrets:
      GCP_WIF_PROVIDER:
        required: false
      GCP_WIF_SA:
        required: false

permissions:
  contents: read
  id-token: write  # OBLIGATOIRE pour OIDC -> WIF

jobs:
  terraform:
    name: Terraform ${{ inputs.action }} (${{ inputs.environment }})
    runs-on: ubuntu-latest

    # Attache le job à un GitHub Environment:
    # - permet Required reviewers (staging/prod)
    # - permet variables et secrets par environnement
    environment: ${{ inputs.environment }}

    # Variables communes:
    # Ces variables viennent des "Environment Variables" de GitHub:
    # Settings -> Environments -> <dev|staging|prod> -> Variables
    env:
      PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      TF_BUCKET: ${{ vars.TF_STATE_BUCKET }}

      # Terraform en CI:
      TF_IN_AUTOMATION: "true"
      TF_INPUT: "false"

    steps:
      # ==========================================================
      # 0) Checkout code
      # ==========================================================
      - name: Checkout
        uses: actions/checkout@v4

      # ==========================================================
      # 1) Contexte (log)
      # ==========================================================
      - name: Print context (debug)
        shell: bash
        run: |
          set -euo pipefail
          echo "== Context =="
          echo "event_name=${GITHUB_EVENT_NAME}"
          echo "ref=${GITHUB_REF}"
          echo "actor=${GITHUB_ACTOR}"
          echo "repo=${GITHUB_REPOSITORY}"
          echo "inputs.environment=${{ inputs.environment }}"
          echo "inputs.action=${{ inputs.action }}"

      # ==========================================================
      # 2) Fail fast : vérifier présence des variables (non sensibles)
      # ==========================================================
      - name: Validate Environment Variables (fail fast)
        shell: bash
        run: |
          set -euo pipefail

          if [[ -z "${PROJECT_ID:-}" ]]; then
            echo "❌ Missing vars.GCP_PROJECT_ID in GitHub Environment '${{ inputs.environment }}'"
            exit 1
          fi

          if [[ -z "${TF_BUCKET:-}" ]]; then
            echo "❌ Missing vars.TF_STATE_BUCKET in GitHub Environment '${{ inputs.environment }}'"
            exit 1
          fi

          echo "✅ PROJECT_ID(raw)='${PROJECT_ID}'"
          echo "✅ TF_BUCKET(raw)='${TF_BUCKET}'"

      # ==========================================================
      # 3) Sanitize inputs (IMPORTANT)
      # ----------------------------------------------------------
      # Problème rencontré: variable copiée/collée avec CR/LF -> bucket invalide
      # => On nettoie / trim AVANT tout appel gcloud / terraform init backend
      # ==========================================================
      - name: Sanitize inputs (PROJECT_ID / TF_BUCKET)
        shell: bash
        run: |
          set -euo pipefail

          PROJECT_ID_CLEAN="$(printf '%s' "${PROJECT_ID:-}" | tr -d '\r\n' | xargs)"
          TF_BUCKET_CLEAN="$(printf '%s' "${TF_BUCKET:-}" | tr -d '\r\n' | xargs)"

          if [[ -z "${PROJECT_ID_CLEAN}" ]]; then
            echo "❌ PROJECT_ID empty after sanitize (check vars.GCP_PROJECT_ID)"
            exit 1
          fi
          if [[ -z "${TF_BUCKET_CLEAN}" ]]; then
            echo "❌ TF_BUCKET empty after sanitize (check vars.TF_STATE_BUCKET)"
            exit 1
          fi

          echo "PROJECT_ID=${PROJECT_ID_CLEAN}" >> "$GITHUB_ENV"
          echo "TF_BUCKET=${TF_BUCKET_CLEAN}"   >> "$GITHUB_ENV"

          echo "✅ PROJECT_ID(clean)='${PROJECT_ID_CLEAN}'"
          echo "✅ TF_BUCKET(clean)='${TF_BUCKET_CLEAN}'"

      # ==========================================================
      # 4) Détection "secrets disponibles ou pas"
      # ----------------------------------------------------------
      # Sur PR fork / Dependabot => GitHub n'injecte PAS les secrets.
      # => auth@v2 échoue si workload_identity_provider vide.
      # => En entreprise: on skip proprement et on sort OK.
      # ==========================================================
      - name: Detect secrets availability
        id: secrets_check
        shell: bash
        run: |
          set -euo pipefail

          if [[ -z "${{ secrets.GCP_WIF_PROVIDER }}" || -z "${{ secrets.GCP_WIF_SA }}" ]]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "⚠️ WIF secrets NOT available (expected for fork PR / Dependabot)."
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
            echo "✅ WIF secrets available."
          fi

      # ==========================================================
      # 5) Auth GCP via WIF (uniquement si secrets dispo)
      # ==========================================================
      - name: Authenticate to Google Cloud (WIF)
        if: steps.secrets_check.outputs.available == 'true'
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_WIF_SA }}
          create_credentials_file: true
          export_environment_variables: true
          # optionnel: plus verbose si besoin
          # token_format: "access_token"

      # ==========================================================
      # 6) Setup gcloud (uniquement si auth OK)
      # ==========================================================
      - name: Setup gcloud
        if: steps.secrets_check.outputs.available == 'true'
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      # ==========================================================
      # 7) Forcer le contexte projet (provider Terraform + gcloud)
      # ==========================================================
      - name: Force project context
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          echo "CLOUDSDK_CORE_PROJECT=${PROJECT_ID}" >> "$GITHUB_ENV"
          echo "GOOGLE_CLOUD_PROJECT=${PROJECT_ID}"  >> "$GITHUB_ENV"
          echo "GOOGLE_PROJECT=${PROJECT_ID}"        >> "$GITHUB_ENV"
          echo "GCP_PROJECT=${PROJECT_ID}"           >> "$GITHUB_ENV"
          echo "Forced PROJECT_ID=${PROJECT_ID}"

      # ==========================================================
      # 8) Debug identité (best effort)
      # ==========================================================
      - name: Verify identity (debug, best effort)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -u
          echo "ADC file: ${GOOGLE_APPLICATION_CREDENTIALS:-<empty>}"
          gcloud version || true
          gcloud config list || true
          gcloud auth list || true
          gcloud projects describe "${PROJECT_ID}" >/dev/null && echo "OK: can read project" || true

      # ==========================================================
      # 9) Debug bucket state (APRES sanitize)
      # ----------------------------------------------------------
      # Problème rencontré:
      # - gcloud storage ls -> 404 bucket n'existe pas
      # - ou bucket invalide (retour à la ligne)
      # => ici, on "describe" le bucket pour fail fast + diagnostic clair
      # ==========================================================
      - name: Debug TF_BUCKET (describe)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          echo "TF_BUCKET='${TF_BUCKET}'"
          echo "TF_BUCKET_LEN=$(printf '%s' "${TF_BUCKET}" | wc -c | tr -d ' ')"
          gcloud storage buckets describe "gs://${TF_BUCKET}" --format="value(name,location,storage_url)"

      # ==========================================================
      # 10) Backend sanity (GCS state) - fail fast
      # ==========================================================
      - name: Backend sanity (GCS state) - fail fast
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          gcloud storage ls "gs://${TF_BUCKET}/" >/dev/null
          echo "OK: bucket reachable: gs://${TF_BUCKET}"

      # ==========================================================
      # 11) Setup Terraform
      # ==========================================================
      - name: Setup Terraform
        if: steps.secrets_check.outputs.available == 'true'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      # ==========================================================
      # 12) FOUNDATION : fmt / init / validate / plan|apply
      # ----------------------------------------------------------
      # IMPORTANT:
      # - terraform fmt -recursive formate aussi les *.tfvars => ça cassait chez toi
      # => on fait fmt UNIQUEMENT sur les *.tf
      # ==========================================================
      - name: Terraform fmt (foundation) - only *.tf
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cd terraform/foundation
          terraform fmt -check -recursive $(find . -type f -name "*.tf" -print)

      - name: Terraform init (foundation)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/foundation init -reconfigure -lockfile=readonly \
            -backend-config="bucket=${TF_BUCKET}" \
            -backend-config="prefix=foundation/${{ inputs.environment }}"

      - name: Terraform validate (foundation)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: terraform -chdir=terraform/foundation validate

      - name: Terraform plan (foundation)
        if: steps.secrets_check.outputs.available == 'true' && inputs.action == 'plan'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/foundation plan -compact-warnings \
            -var-file=envs/${{ inputs.environment }}/terraform.tfvars

      - name: Terraform apply (foundation)
        if: steps.secrets_check.outputs.available == 'true' && inputs.action == 'apply'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/foundation apply -auto-approve -compact-warnings \
            -var-file=envs/${{ inputs.environment }}/terraform.tfvars

      # ==========================================================
      # 13) LAKEHOUSE : fmt / init / validate / plan|apply
      # ==========================================================
      - name: Terraform fmt (lakehouse) - only *.tf
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cd terraform/lakehouse
          terraform fmt -check -recursive $(find . -type f -name "*.tf" -print)

      - name: Terraform init (lakehouse)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/lakehouse init -reconfigure -lockfile=readonly \
            -backend-config="bucket=${TF_BUCKET}" \
            -backend-config="prefix=lakehouse/${{ inputs.environment }}"

      - name: Terraform validate (lakehouse)
        if: steps.secrets_check.outputs.available == 'true'
        shell: bash
        run: terraform -chdir=terraform/lakehouse validate

      - name: Terraform plan (lakehouse)
        if: steps.secrets_check.outputs.available == 'true' && inputs.action == 'plan'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/lakehouse plan -compact-warnings \
            -var-file=envs/${{ inputs.environment }}/terraform.tfvars

      - name: Terraform apply (lakehouse)
        if: steps.secrets_check.outputs.available == 'true' && inputs.action == 'apply'
        shell: bash
        run: |
          set -euo pipefail
          terraform -chdir=terraform/lakehouse apply -auto-approve -compact-warnings \
            -var-file=envs/${{ inputs.environment }}/terraform.tfvars

      # ==========================================================
      # 14) Mode "secrets non dispo" : on informe et on sort OK
      # ==========================================================
      - name: Skip message (no secrets)
        if: steps.secrets_check.outputs.available == 'false'
        shell: bash
        run: |
          echo "ℹ️ Skipping Terraform because WIF secrets are not available (fork PR / Dependabot)."
          echo "Enterprise-standard behavior: avoid leaking secrets."